let object = {left:1, right:0};


/* Retrieve all object keys */
// console.log(Object.keys(object));

/* Retrieve all object value */
// console.log(Object.values(object));

/* delete an object key and value */
// delete object.left;

/* Check a value in object */
// console.log("left" in object);

/* Assign key and value into object - Object.assign(objname, {} )*/
// Object.assign(object, {a:1, b:2});
//  console.log(Object.values(object));

/********************************* Object prototype ********************************/

// let rabbit = {};
// rabbit.speak = function(line) {
// console.log(`The rabbit says '${line}'`); };
// rabbit.speak("I'm alive.");

let empty = {};
console.log(Object.prototype(key));